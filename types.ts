
export interface Property {
  date: string;
  name: string;
  address: string;
  lat: number;
  lng: number;
  displayAddress: string;
  price: string;
  branch: string;
}

export type BranchColor = 'blue' | 'red' | 'green';
